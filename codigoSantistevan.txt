3 + 4 * 10 a
   + -20 *2 / 9 % 23 // Esto en adelante es un comentario/// + (9) %
   while(asd > 3 && ! (asd < 20)) {
   /* Esto es otro comentario */ int j; //Los comentarios con /**/ solo funcionan en 1 linea por como se lee el archivo.
    asd = asd - 1;
    }